apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: dev-workspace
  namespace: dev

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: manual

  resources:
    requests:
      storage: 20Gi
