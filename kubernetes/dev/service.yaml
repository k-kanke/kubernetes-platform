apiVersion: v1
kind: Service

metadata:
  name: dev-workspace
  namespace: dev

spec:
  selector:
    app: dev-workspace

  ports:
    - name: http
      port: 80
      targetPort: 3000

  type: ClusterIP
