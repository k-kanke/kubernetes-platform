apiVersion: v1
kind: Service

metadata:
  name: dev-workspace
  namespace: dev

spec:
  selector:
    app: dev-workspace

  ports:
    - name: http
      port: 8080
      targetPort: 8080

  type: ClusterIP
