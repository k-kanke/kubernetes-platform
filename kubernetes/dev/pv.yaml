apiVersion: v1
kind: PersistentVolume

metadata:
  name: dev-workspace-pv

spec:
  capacity:
    storage: 20Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  storageClassName: manual

  hostPath:
    path: /data/dev-workspace
