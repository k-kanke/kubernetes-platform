apiVersion: v1
kind: Service

metadata:
  name: web-app
  namespace: apps

spec:
  type: LoadBalancer
  loadBalancerClass: tailscale

  selector:
    app: web-app

  ports:
    - name: http
      port: 80
      targetPort: 80

